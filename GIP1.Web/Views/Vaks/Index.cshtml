@model IEnumerable<GIP1.Web.Entities.Vak>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vaknaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Studiepunten)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vaknaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Studiepunten)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Vakcode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Vakcode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Vakcode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
