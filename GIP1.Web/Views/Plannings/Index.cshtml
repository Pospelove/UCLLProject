@model IEnumerable<GIP1.Web.Entities.Planning>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Datumtijd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Datumtijd)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Planningcode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Planningcode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Planningcode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
